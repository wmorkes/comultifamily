Last run: 09/04/2026

# Performance Audit — comultifamily.com

## 1. Image Format Coverage

`site/images/` (incl. `deals/`, `listings/`) contains 56 files total. All raster photo assets are `.webp`. Non-webp files found:

- `site/images/README.md` — not an image, no action needed

No `.jpg`, `.jpeg`, or `.png` raster files found anywhere under `site/images/`. `colliers-logo.svg` and other SVGs are the expected exceptions. **WebP migration remains fully intact — no regressions since the last confirmed migration (2026-08-20).**

## 2. External CDN Script Check

CLAUDE.md bans external JS libraries on the main site unless essential. Found:

| Script | Pages |
|---|---|
| `unpkg.com/leaflet@1.9.4/dist/leaflet.js` | `dashboards/affordable-housing/`, `dashboards/on-market/`, `dashboards/loan-monitor/`, `dashboards/pipeline/` (+ `mobile/`), `dashboards/markets/{denver-metro,greeley,mountain-towns,fort-collins,boulder,pueblo,colorado-springs,rural-tertiary-co,wyoming,western-slope}/` |
| `maps.googleapis.com/maps/api/js` (Google Maps, key `AIzaSyB6e_ha0ekgci_nHcjhn8kJDdk_e6R9Vus`) | `dashboards/geocode/` |

All 15 hits are in `site/dashboards/**` (internal, token-gated reporting tools built via the db-tools pipeline, not the public marketing site). **No external CDN scripts on any public marketing page** (home, listings, markets, team, services, contact, track-record, resources, affordable/distressed). Leaflet and Google Maps are functionally required for the map-based dashboards, so this is consistent with the "unless essential" exception — flagging for visibility only, no action recommended.

## 3. Inline `<script>` Block Check (>10 lines)

Public marketing site pages:

| Page | Lines | Notes |
|---|---|---|
| `site/contact.html` | 2 blocks, 11 + 23 lines | Page-specific: URL-hash dropdown preselect (logic is duplicated across *both* blocks — could be de-duped in-page) + Netlify form success handling. Small, page-specific; not a strong extraction candidate but the duplicate hash-preselect logic between the two blocks could be merged. |
| `site/listings.html` | 161 lines (lines 131–292) | Fetches and parses the Listings CSV directly (own hand-rolled `parseCSV`), builds fallback static data, and renders property cards — all inline. `site/js/deal-map.js` (used by track-record) does *not* contain shared CSV-parsing logic, so this isn't strict duplication, but it's the single largest inline block on the public site and a natural candidate to extract into a new `site/js/listings.js` for maintainability and caching. |

Dashboard pages (`site/dashboards/**`) carry much larger inline blocks (up to ~1,411 lines each, repeated near-identically across the 10 `dashboards/markets/*` pages and the `chfa`/`pipeline`/`loan-monitor`/`sales-by-year`/`deliveries-by-year` dashboards, each with a matching `mobile/` variant). These are internal, token-gated, per-dashboard data/render logic generated by the db-tools build pipeline — out of scope for the public-site CLAUDE.md constraints, but flagged since the `dashboards/markets/*` 1,411-line block is near-byte-identical across all 10 market dashboards and could be consolidated into one shared dashboard JS file if dashboard load time ever becomes a concern.

## 4. CSS Size Trend

`site/css/style.css`: **96,748 bytes / 2,923 lines**

No prior `perf-report.md` existed before this run, so this is the **baseline** for future comparisons — unchanged from the pre-audit reference figures. Next run should diff against these numbers.

---
*Next run: re-check `site/images/` for new raster additions, re-grep for external `<script src>` tags, and diff `style.css` size/line count against the baseline above.*
